/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com'], // Add this if you're using placeholder images
    },
  };
  
  export default nextConfig;